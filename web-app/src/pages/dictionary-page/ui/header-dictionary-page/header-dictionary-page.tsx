import s from "./header-dictionary-page.module.scss";

export const HeaderDictionaryPage = () => {
  return (
    <header className={s.header}>
      <div className={s.buttonNewWord}>+</div>
    </header>
  );
};
